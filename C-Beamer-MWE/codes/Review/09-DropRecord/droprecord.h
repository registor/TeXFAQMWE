#ifndef DROPRECORD_H_INCLUDED
#define DROPRECORD_H_INCLUDED

#include <stdlib.h>
#include "define.h"

void run();
size_t getDataSize(Student **);
size_t dropRecord(Student**, size_t);

#endif // DROPRECORD_H_INCLUDED
